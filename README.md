IPC (Connection between Python and ++)

This is currently linux version and implemented with ZeroMQ so it is necessary to set up ZeroMQ environment.

To simulate, 'make' to compile and ./server to run it


